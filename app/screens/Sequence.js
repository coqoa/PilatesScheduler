import React from 'react';
import { Text, View } from 'react-native';

const Sequences = () => {
    return (
        <View>
            <Text>Sequences</Text>
        </View>
    );
};

export default Sequences;